spring.datasource.url=jdbc:postgresql://localhost:5432/vivekDB
spring.datasource.username=postgres
spring.datasource.password=vivek123
spring.datasource.driver-class-name=org.postgresql.Driver

#query

example.queries.selectAll = SELECT * from employee
example.queries.findById = SELECT * from employee where id=?
example.queries.updateByValue = INSERT INTO employee(name,mobileNo) VALUES (?,?)

